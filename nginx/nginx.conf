events {}

http {
    server {
        listen 80;

        # ⚡ frontend (Vue build)
        location / {
            root /usr/share/nginx/html;
            try_files $uri /index.html;
        }

        # 🔗 backend API
        location /api/ {
            proxy_pass http://api:5000/;
        }
    }
}
